# --- Web framework / server -----------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9      # required by FastAPI for UploadFile / form parsing
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0

# --- OCR / PDF ingestion -----------------------------------------------------
pytesseract>=0.3.10
pdf2image>=1.17
opencv-python-headless>=4.9

# --- Embeddings / reranking (local models, CPU) ------------------------------
sentence-transformers>=3.0

# --- Vector store / sparse retrieval -----------------------------------------
chromadb==0.5.5
rank_bm25>=0.2.2

# --- Query understanding ------------------------------------------------------
langdetect>=1.0.9
tiktoken>=0.7

# --- LLM ----------------------------------------------------------------------
groq>=0.9

# --- Misc ----------------------------------------------------------------------
tqdm>=4.66
